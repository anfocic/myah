# Sample project

`lib/` holds five small support modules (auth, cache, config, logger,
parser). The eval task asks the agent to summarize each — delegating
the investigation to a subagent so the parent's context stays clean.
