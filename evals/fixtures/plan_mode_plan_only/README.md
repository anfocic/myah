# Workspace

Empty project directory. The eval task asks you to *plan* an
implementation; the plan-mode gate blocks mutating tools, so your
response should describe the proposed code and tests in prose, not
create files.
